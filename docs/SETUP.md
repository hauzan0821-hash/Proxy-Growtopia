# Setup

1. Install .NET 8 SDK.
2. Copy `Proxy/appsettings.example.json` menjadi `Proxy/appsettings.json`.
3. Isi data KeyAuth (`AppName`, `OwnerId`, `Version`, `Secret` jika perlu).
4. Jalankan dari Visual Studio (`proxy1.sln`) atau `Proxy/start-proxy.bat`.
