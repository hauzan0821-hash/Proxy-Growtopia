# Lua files

Folder ini berisi versi Lua sederhana untuk TCP proxy.

## File
- `config.lua` -> konfigurasi proxy + placeholder KeyAuth
- `proxy.lua` -> TCP forwarder (butuh LuaSocket)

## Jalankan
1. Install Lua + LuaSocket
2. Edit `config.lua`
3. Jalankan `lua proxy.lua`
